Unit test execution:
`uv run pytest ...`

Check static python code and compilation
`make check`

Format code
`make format`

IDE alignment (Cursor + Claude Code) and quality hooks: `docs/IDE-SETUP.md`, `docs/PATTERNS/INDEX.md`, root `CLAUDE.md`, and `gain.json` (Rosetta doc paths).